org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# Optional Esri raster basemaps. Supply ESRI_API_KEY in uncommitted
# `local.properties`, as a Gradle property, or through the environment. No key
# is required for imported PDF/GeoPDF, MBTiles, or keyless OpenTopoMap use.
